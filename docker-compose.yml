services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: math
      POSTGRES_PASSWORD: math
      POSTGRES_DB: math4
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U math -d math4"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pg_data:
